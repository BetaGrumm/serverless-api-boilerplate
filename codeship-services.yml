app:
  build:
    image: betagrumm/serverless-demo
    dockerfile: Dockerfile
  depends_on: mongodb

mongodb:
  image: mongo:3.4
  command: mongod --httpinterface --rest
  ports:
    - "27017:27017"
  cached: true